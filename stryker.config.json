{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "mutate": [
    "packages/core/src/**/*.ts",
    "packages/graph/src/**/*.ts",
    "!packages/**/*.d.ts"
  ],
  "coverageAnalysis": "perTest",
  "thresholds": {
    "high": 80,
    "low": 68,
    "break": 65
  },
  "plugins": [
    "@stryker-mutator/vitest-runner"
  ],
  "reporters": [
    "json",
    "clear-text",
    "progress"
  ],
  "jsonReporter": {
    "fileName": "reports/mutation/mutation.json"
  },
  "_thresholds_note": "break sits below the measured score so a regression fails and an aspiration does not. Measured 2026-09-01: 68.35% overall, 71.1% over covered code, with 117 of 3017 mutants unreached. The gap to 100% is largely mutants no test can distinguish \u2014 a changed zod message, a default nothing reads \u2014 and chasing those writes tests that assert nothing."
}
