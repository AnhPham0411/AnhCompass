**What changes**

**Why**

---

- [ ] `pnpm -r build && pnpm -r typecheck && pnpm lint && pnpm test && pnpm bench -- --graph` all pass
- [ ] An engine change comes with a benchmark case, labelled from the rule, written before the fix
- [ ] A bug the holdout corpus found also has a unit test
- [ ] BENCHMARKS.md updated if any number moved — including numbers that got worse
