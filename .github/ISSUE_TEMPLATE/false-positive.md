---
name: False positive
about: AnhCompass reported a violation on correct code
labels: false-positive
---

**The rule** — paste the intent frontmatter:

```yaml
```

**The diff or file** that was reported:

```
```

**Why it is correct** — one sentence on what the code actually does.

**Version:** `anhcompass --version`
**Engine:** deterministic / graph / semantic (from the report)
