---
name: Missed violation
about: Code breaks a rule and AnhCompass reported pass
labels: false-negative
---

**The rule** — paste the intent frontmatter:

```yaml
```

**The code that breaks it:**

```
```

**Was the answer `pass` or `uncertain`?** `uncertain` means the engine knew it could not
see the input, which is a different bug from a confident miss.

**Version:** `anhcompass --version`
