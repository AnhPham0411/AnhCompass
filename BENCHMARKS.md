# AnhCompass Benchmark Results

> How to reproduce: `pnpm bench` (deterministic, free) · `pnpm bench -- --semantic` (needs an LLM API key).
> Case corpus: `benchmarks/cases/` — labeled by intended rule semantics, adversarially audited.
> Categories: **correct** (clean code), **wrong** (clear violations), **edge** (tricky-but-decidable), **ai-generated** (realistic multi-file coding-agent diffs with violations buried or absent).

_Results pending first full run — see `benchmarks/results/report.md` after running._
